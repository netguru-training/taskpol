- unless @activities.empty?
  %table.table.table-striped
    %tbody
      - @activities.each do |activity|
        %tr
          %td.activity
            = link_to "#{activity.owner.firstname} #{activity.owner.lastname}", activity.owner if activity.owner
            = render_activity activity
